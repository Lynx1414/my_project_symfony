{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}


{# {{ form_start(form_ref) }}
    {{ form_widget(form_ref) }}
    <button class="btn">{{ button_label|default('Search') }}</button>
{{ form_end(form_ref) }} #}