{% extends 'base.html.twig' %}

{% block title %}Informations d'un Client{% endblock %}

{% block body %}
    <h1>Informations d'un client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ client.nomClient }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ client.prenomClient}}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ client.mailClient}}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ client.villeClient}}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_clients_all') }}">retour à la liste</a>

    {# <a href="{{ path('app_clients_edit', {'id': client.id}) }}">edit</a> #}

    {# {{ include('clients/_delete_form.html.twig') }} #}
{% endblock %}