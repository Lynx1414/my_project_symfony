{% extends 'base.html.twig' %}

{% block title %}Edit Enseignes{% endblock %}

{% block body %}
    <h1>Edit Enseignes</h1>

    {{ include('enseignes/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_enseignes_index') }}">back to list</a>

    {{ include('enseignes/_delete_form.html.twig') }}
{% endblock %}
