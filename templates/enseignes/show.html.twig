{% extends 'base.html.twig' %}

{% block title %}Enseignes{% endblock %}

{% block body %}
    <h1>Enseignes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enseigne.id }}</td>
            </tr>
            <tr>
                <th>Nom_enseigne</th>
                <td>{{ enseigne.nomEnseigne }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ enseigne.ville }}</td>
            </tr>
            <tr>
                <th>Nbre_employes</th>
                <td>{{ enseigne.nbreEmployes }}</td>
            </tr>
            <tr>
                <th>Chiffre_affaire_annuel_HT</th>
                <td>{{ enseigne.chiffreAffaireAnnuelHT }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_enseignes_index') }}">back to list</a>

    <a href="{{ path('app_enseignes_edit', {'id': enseigne.id}) }}">edit</a>

    {{ include('enseignes/_delete_form.html.twig') }}
{% endblock %}
